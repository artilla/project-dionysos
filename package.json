{
  "name": "forest-gap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "dev": "node server/local.mjs",
    "start": "node server/local.mjs",
    "build": "vite build && vite build --config vite.server.config.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "leaflet": "1.9.4",
    "linkedom": "^0.18.13",
    "tough-cookie": "^6.0.2"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "^0.2.0",
    "vite": "^8.2.2"
  }
}
